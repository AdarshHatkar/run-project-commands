

// Version information
export const VERSION = '1.0.0';

// Utility functions
export function getPackageInfo() {
  return {
    name: 'Run Project Commands',
    version: VERSION,
    description: 'A CLI tool to run project commands'
  };
}
